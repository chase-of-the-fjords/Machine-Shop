/**
 * SAVE BOX DEFAULT EXPORT
 * 
 * @returns A JSX representation of the Save Box.
 */
export default function SaveBox() {

        // JSX (RETURN VALUE)

    return <div className="fixed w-full h-[120%] bg-black opacity-50" />

}